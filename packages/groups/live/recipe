name=live
version=1.0.0
release=1
arch=x86_64
license="GPL-3.0"
description="Live ISO environment with XFCE"
source=""
sha256=""
depends="desktop,wireless-regdb,mkfontscale"
# NOTA: calamares non è ancora pacchettizzato nel repo — va riaggiunto qui
# quando avrà una ricetta completa (incluse tutte le dipendenze: kpmcore,
# yaml-cpp, boost, libpwquality, parted, e2fsprogs, dosfstools) e un
# .kpkg pubblicato.

build() {
    :
}

package() {
    :
}
